name: build-all

# 仓库写入权限
permissions:
  contents: write
  
on:
  workflow_dispatch:
jobs:
  do-nothing:
    runs-on: ubuntu-latest
    steps:
      - name: Do nothing
        run: echo "This workflow does nothing"
